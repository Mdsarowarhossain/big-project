<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item><shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
            <solid android:color="#ffffff" />

            <!-- <size android:height="120dp" android:width="120dp" /> -->
            <corners android:bottomLeftRadius="14dp" android:bottomRightRadius="14dp" android:topLeftRadius="14dp" android:topRightRadius="14dp" />
        </shape></item>
    <item> <!-- A red ripple masked against an opaque rectangle. -->
<ripple android:color="@color/app_main_color">
            <item android:id="@android:id/mask" android:drawable="@android:color/white"/>
        </ripple></item>

</layer-list>
